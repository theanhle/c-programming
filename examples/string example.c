/* write a function that takes as input a table
 * consisting of two string arrays and removes the
 * duplicate records
 */

#include <stdio.h>
#include <string.h>

int remove_duplicates(char name[][31], int age[], int *n)
{
	unsigned char mark[*n] = {0};
	char unique_names[*n][31];

}

int main()
{
	return 0;
}

